<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowBackground">@color/grey_300</item>

        <!--Ripple Color-->
        <item name="colorControlHighlight">@color/colorPrimary</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--Toolbar Overflow button yellow-->
    <style name="MainMenuOverflowColor" parent="AppTheme.NoActionBar">
        <item name="android:textColorSecondary">@color/colorYellow</item>
        <!--Ripple Color-->
        <item name="colorControlHighlight">@color/colorYellow</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <!-- Dark Buttons -->
    <style name="ThemeOverlay.MyDarkButton" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorButtonNormal">@color/colorPrimary</item>
        <item name="android:layout_marginRight">4dp</item>
        <item name="android:layout_marginLeft">4dp</item>
        <!--Ripple Color-->
        <!--<item name="colorControlHighlight">@color/colorYellow</item>-->
    </style>

    <!--Main Activity Buttons-->
    <style name="ThemeMainButtons">
        <!--Ripple Color-->
        <item name="colorControlHighlight">@color/colorYellow</item>
    </style>

    <!--Main Activity Urgent Button-->
    <style name="ThemeMainUrgentButtons">
        <!--Ripple Color-->
        <item name="colorControlHighlight">@color/colorRed</item>
    </style>

    <!---->
    <style name="ThemeOverlay.MyTextDetail" parent="AppTheme">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:fadeScrollbars">true</item>
        <item name="android:gravity">center</item>
        <item name="android:maxLines">5</item>
        <item name="android:padding">4dp</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:textSize">14sp</item>
    </style>

    <!---->
    <style name="ThemeOverlay.MyTitleText" parent="AppTheme">
        <item name="android:gravity">center</item>
        <item name="android:textSize">36sp</item>
    </style>

</resources>
